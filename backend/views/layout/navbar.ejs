<nav class="navbar navbar-expand-lg navbar-light navbar_careme">
  <a class="navbar-brand" href="/">
    <img class="navbar_logo" src="/images/logo3.png" alt="">
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
    aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="mr-auto"></ul>
    <ul class="navbar-nav navbar-right navbar_list">
      <li class="nav-item active">
        <a class="nav-link" href="#">게시판
          <span class="sr-only">(current)</span>
        </a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="/erp/main/main">관리 프로그램
          <span class="sr-only">(current)</span>
        </a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="/adoptRegister/adoptRegister">입양 신청
          <span class="sr-only">(current)</span>
        </a>
      </li>
    </ul>
    <div class="">
      <% if(csession==0) { %>
        <button class="btn btn-outline-success" data-toggle="modal" data-target="#modal_login">로그인</button>
        <% }else{ %>
          <div class="dropdown">
            <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true"
              aria-expanded="false">
              <%= csession.name %>님
            </button>
            <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
              <a class="dropdown-item" href="/user/action/logout">Logout</a>
            </div>
          </div>
          <% } %>
    </div>
  </div>
</nav>
<% if(csession==0){ %>
  <% include ./modalLogin.ejs %>
    <% } %>